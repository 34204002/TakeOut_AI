<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">

    <insert id="add">
        insert into sky_take_out.shopping_cart (user_id, dish_id, setmeal_id, dish_flavor, name, image, number, amount, create_time)
        values (#{userId}, #{dishId}, #{setmealId}, #{dishFlavor}, #{name}, #{image}, #{number}, #{amount}, #{createTime})
    </insert>
    <insert id="insertBatch">
        insert into sky_take_out.shopping_cart (user_id, dish_id, setmeal_id, dish_flavor, name, image, number, amount, create_time)
        values
        <foreach collection="list" item="shoppingCart" separator=",">
            (#{shoppingCart.userId}, #{shoppingCart.dishId}, #{shoppingCart.setmealId}, #{shoppingCart.dishFlavor}, #{shoppingCart.name}, #{shoppingCart.image}, #{shoppingCart.number}, #{shoppingCart.amount}, #{shoppingCart.createTime})
        </foreach>
    </insert>

    <select id="list" parameterType="com.sky.entity.ShoppingCart" resultType="com.sky.entity.ShoppingCart">
        select * from sky_take_out.shopping_cart
        <where>
            user_id = #{userId}
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
        </where>
    </select>

    <update id="updateNumberById">
        update sky_take_out.shopping_cart
        set number = #{number}
        where id = #{id}
    </update>

    <delete id="sub">
        delete from sky_take_out.shopping_cart
                <where>
                    user_id=#{userId}
                    <if test="dishId != null">
                        and dish_id=#{dishId}
                    </if>
                    <if test="setmealId != null">
                        and setmeal_id= #{setmealId}
                    </if>
                    <if test="dishFlavor != null">
                        and dish_flavor= #{dishFlavor}
                    </if>
                </where>
    </delete>
    <delete id="deleteByUserId">
        delete from sky_take_out.shopping_cart
        where user_id = #{userId}
    </delete>
</mapper>